import { getRawInput, splitOnNewLine, splitOnWhitespace } from '../util';

const part1 = (input: string[]) => {};

const part2 = (input: any) => {
  //
};

part1(splitOnNewLine(getRawInput('day07')));
// part2(splitOnNewLine(getRawInput('day07')));
